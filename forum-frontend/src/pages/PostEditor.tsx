import PostForm from "../components/PostForm"; // Adjust the import path as necessary

import React from "react";

const CreatePostPage: React.FC = () => {
    return (
        <div>
            <PostForm />
        </div>
    );
};

export default CreatePostPage;
